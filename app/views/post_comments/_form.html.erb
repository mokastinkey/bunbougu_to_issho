<%= form_with(model: [post_bungu, post_comment] ) do |f| %>
  <div>
    <%= f.text_area :content, placeholder: "コメントはこちら !", class: "comment-text-area", wrap: "hard", cols: "20" %>
  </div>
  <div class="comment-action">
    <%= f.submit "送信" %>
  </div>
<% end %>